#include <string.h>
#include <stdio.h>
#include "3-calc.h"

int main (int argc, *argv[])
{
	int n1, n2;

	char o;

	if (argc != 4)
	{
		printf("Error\n");
		exit(98);
	}
	o1 = atoi(argv[1][0]);
	o2 = atoi(argv[])

	return (0);
}
